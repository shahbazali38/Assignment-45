let userName1 = ["Shayan(admin)", "Najam", "Arman", "Dilawar", "Alisher"];
if (userName1.length === 0) {
    console.log("We need to find some users!");   
} else {
    console.log("There are users available!");
}

let userName2 = [];
if (userName2.length === 0) {
    console.log("We need to find some users!");   
} else {
    console.log("There are users available!");
}
